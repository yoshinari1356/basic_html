name: PR Preview (GitHub Pages)

on:
  issue_comment:
    types: [created]
  pull_request:
    types: [opened, synchronize, reopened, closed]

permissions:
  contents: write
  pull-requests: write
  issues: write

env:
  # 公開したいディレクトリ（repo直下なら "."、docs 配下なら "docs" など）
  SITE_DIR: "."

jobs:
  # ───────────────────────────────────────────────────────────────────
  # deploy: PR push 時の自動デプロイ / /preview deploy コマンド
  # ───────────────────────────────────────────────────────────────────
  deploy:
    if: |
      (github.event_name == 'pull_request' && github.event.action != 'closed') ||
      (github.event_name == 'issue_comment' &&
       github.event.issue.pull_request != null &&
       contains(github.event.comment.body, '/preview deploy'))
    runs-on: ubuntu-latest
    concurrency:
      group: pr-preview-deploy-${{ github.event.issue.number || github.event.pull_request.number }}
      cancel-in-progress: true

    steps:
      # コメントコマンド時のみ権限チェック
      - name: Authorize (issue_comment only)
        if: github.event_name == 'issue_comment'
        uses: actions/github-script@v7
        with:
          script: |
            const login = context.payload.comment.user.login;
            const prNumber = context.payload.issue.number;
            const { data } = await github.rest.repos.getCollaboratorPermissionLevel({
              owner: context.repo.owner,
              repo: context.repo.repo,
              username: login,
            });
            const allowed = ["write", "maintain", "admin"].includes(data.permission);
            if (!allowed) {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: prNumber,
                body: `⛔ 権限不足のため実行できません。\n\n実行者: @${login}\n権限: ${data.permission}\n必要: write / maintain / admin`,
              });
              core.setFailed("Insufficient permissions");
            }

      # PR番号の取得（イベント種別に応じて）
      - name: Get PR number
        id: pr_num
        run: |
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            echo "number=${{ github.event.pull_request.number }}" >> $GITHUB_OUTPUT
          else
            echo "number=${{ github.event.issue.number }}" >> $GITHUB_OUTPUT
          fi

      # issue_comment 時のみ PR の head ref を API 取得
      - name: Get PR head ref (issue_comment only)
        if: github.event_name == 'issue_comment'
        id: pr_api
        uses: actions/github-script@v7
        with:
          script: |
            const prNumber = context.payload.issue.number;
            const { data: pr } = await github.rest.pulls.get({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: prNumber,
            });
            core.setOutput("head_ref", pr.head.ref);
            core.setOutput("head_repo", pr.head.repo.full_name);

      - name: Checkout PR branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref || steps.pr_api.outputs.head_ref }}
          repository: ${{ github.event.pull_request.head.repo.full_name || steps.pr_api.outputs.head_repo }}

      - name: Prepare site files
        shell: bash
        run: |
          mkdir -p .preview_out
          rsync -av --delete \
            --exclude ".git/" \
            --exclude ".github/" \
            --exclude "node_modules/" \
            --exclude ".env" \
            --exclude ".env.*" \
            --exclude "*.pem" \
            --exclude "*.key" \
            --exclude "secrets.*" \
            "${SITE_DIR}/" ".preview_out/"

          # SPA のリロード対策
          if [ ! -f ".preview_out/404.html" ] && [ -f ".preview_out/index.html" ]; then
            cp ".preview_out/index.html" ".preview_out/404.html"
          fi

      - name: Deploy to gh-pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages
          folder: .preview_out
          target-folder: pr-${{ steps.pr_num.outputs.number }}
          clean: false

      - name: Comment preview URL on PR
        uses: actions/github-script@v7
        with:
          script: |
            const prNumber = Number("${{ steps.pr_num.outputs.number }}");
            const isAuto = "${{ github.event_name }}" === "pull_request";
            const trigger = isAuto ? "自動デプロイ" : "`/preview deploy`";
            const url = `https://${context.repo.owner}.github.io/${context.repo.repo}/pr-${prNumber}/`;
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: prNumber,
              body: `✅ Preview deployed（${trigger}）\n\n🔗 ${url}`,
            });

  # ───────────────────────────────────────────────────────────────────
  # cleanup: PR クローズ時の自動削除 / /preview cleanup コマンド
  # ───────────────────────────────────────────────────────────────────
  cleanup:
    if: |
      (github.event_name == 'pull_request' && github.event.action == 'closed') ||
      (github.event_name == 'issue_comment' &&
       github.event.issue.pull_request != null &&
       contains(github.event.comment.body, '/preview cleanup'))
    runs-on: ubuntu-latest
    concurrency:
      group: pr-preview-cleanup-${{ github.event.issue.number || github.event.pull_request.number }}
      cancel-in-progress: true

    steps:
      # コメントコマンド時のみ権限チェック
      - name: Authorize (issue_comment only)
        if: github.event_name == 'issue_comment'
        uses: actions/github-script@v7
        with:
          script: |
            const login = context.payload.comment.user.login;
            const prNumber = context.payload.issue.number;
            const { data } = await github.rest.repos.getCollaboratorPermissionLevel({
              owner: context.repo.owner,
              repo: context.repo.repo,
              username: login,
            });
            const allowed = ["write", "maintain", "admin"].includes(data.permission);
            if (!allowed) {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: prNumber,
                body: `⛔ 権限不足のため実行できません。\n\n実行者: @${login}\n権限: ${data.permission}\n必要: write / maintain / admin`,
              });
              core.setFailed("Insufficient permissions");
            }

      # PR番号の取得（イベント種別に応じて）
      - name: Get PR number
        id: pr_num
        run: |
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            echo "number=${{ github.event.pull_request.number }}" >> $GITHUB_OUTPUT
          else
            echo "number=${{ github.event.issue.number }}" >> $GITHUB_OUTPUT
          fi

      - name: Cleanup preview folder on gh-pages
        shell: bash
        run: |
          git clone --depth=1 --branch gh-pages \
            "https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git" ghp

          cd ghp
          PR_DIR="pr-${{ steps.pr_num.outputs.number }}"

          if [ ! -d "$PR_DIR" ]; then
            echo "No preview found for $PR_DIR. Skipping."
            exit 0
          fi

          rm -rf "$PR_DIR"
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A

          if git diff --cached --quiet; then
            echo "Nothing to cleanup"
            exit 0
          fi

          git commit -m "chore: remove preview for PR #${{ steps.pr_num.outputs.number }}"
          git push

      - name: Comment cleanup result on PR
        uses: actions/github-script@v7
        with:
          script: |
            const prNumber = Number("${{ steps.pr_num.outputs.number }}");
            const isAuto = "${{ github.event_name }}" === "pull_request";
            const trigger = isAuto ? "PR クローズ（自動）" : "`/preview cleanup`";
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: prNumber,
              body: `🧹 Preview cleaned（${trigger}）\n\npr-${prNumber} を削除しました。`,
            });
